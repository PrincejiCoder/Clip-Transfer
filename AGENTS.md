**AGENTS**
- **Run server**: `cargo run --release` (default bind 0.0.0.0:8080). Override with env/CLI: `LINKDROP_BIND`, `LINKDROP_PORT`, `--bind`, `--port`.
- **Docker**: `docker build -t linkdrop . && docker run -d -p 8080:8080 linkdrop`. No extra steps needed; image copies built binary.
- **Tests**: `cargo test --verbose`. All unit/integration tests run.
- **Data storage**: defaults to `LINKDROP_data` directory (`LINKDROP_DATA_DIR`). Contains SQLite DB (or JSON if `LINKDROP_JSON_DB=true`). Ensure directory is writable.
- **Routing**:
  - `/` – home page.
  - `/{slug}` – view existing paste, edit mode (if `allow_edit`), or create page when slug missing.
  - `/{slug}?created=true` – share UI with QR code.
  - `/raw/{slug}` – plain‑text raw content.
  - Reserved slugs (`static`, `public`, `favicon.ico`, `raw`, `api`, `admin`). Cannot be used for pastas.
- **Slug validation**: regex `^[a-z0-9-_]{3,50}$`. Normalized (lower‑case, spaces→`-`, trimmed non‑alphanum).
- **Env/CLI flags** (most common):
  - `LINKDROP_BASIC_AUTH_USERNAME` / `..._PASSWORD` – HTTP basic auth.
  - `LINKDROP_ADMIN_USERNAME` / `..._PASSWORD` – admin UI.
  - `LINKDROP_PUBLIC_PATH` – URL prefix when behind reverse proxy.
  - `LINKDROP_HIDE_HEADER`, `LINKDROP_HIDE_FOOTER`, `LINKDROP_HIDE_LOGO` – UI toggles.
  - `LINKDROP_ENABLE_BURN_AFTER`, `LINKDROP_DEFAULT_BURN_AFTER` – burn‑after‑read support.
  - `LINKDROP_DEFAULT_EXPIRY`, `LINKDROP_MAX_EXPIRY` – expiry presets.
  - `LINKDROP_DISABLE_TELEMETRY` – disables telemetry thread.
  - `LINKDROP_THREADS` – worker count for Actix.
  - `LINKDROP_GC_DAYS` – days after which unused pastas are pruned.
  - `LINKDROP_QR` – enable QR code generation (requires `LINKDROP_PUBLIC_PATH`).
  - `LINKDROP_CUSTOM_CSS` – URL to custom stylesheet.
- **Feature flags** (via env):
  - `LINKDROP_JSON_DB` – use JSON DB instead of SQLite.
  - `LINKDROP_NO_FILE_UPLOAD` – disable file uploads.
  - `LINKDROP_READONLY` – server‑wide upload lock; `LINKDROP_UPLOADER_PASSWORD` required to create.
  - `LINKDROP_ENABLE_READONLY` – per‑pasta read‑only option in UI.
  - `LINKDROP_ENCRYPTION_CLIENT_SIDE` / `LINKDROP_ENCRYPTION_SERVER_SIDE` – client‑side / server‑side encryption.
- **Telemetry**: started automatically unless `--disable-telemetry`/`LINKDROP_DISABLE_TELEMETRY`.
- **Cleanup**: background thread removes expired pastas every minute.
- **CLI usage**: All flags are generated by `clap`; `--help` shows full list.
- **Important gotchas**:
  - Slug must not conflict with reserved list; otherwise `403`.
  - Large payloads (>1 MiB) are rejected (`413`).
  - When `public_path` is set, generated share URLs use it; otherwise fallback to `http://localhost:PORT`.
  - Changing a flag after data is created does not retroactively affect existing pastas (e.g., encryption, privacy).
- **Common dev workflow**:
  1. `cargo build --release`
  2. `cargo run --release` (or Docker)
  3. Open `http://localhost:8080`.
  4. Run `cargo test` before committing.
- **CI**: GitHub Actions run `cargo build` then `cargo test` on push/PR.
